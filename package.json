{
  "name": "demo-pipeline",
  "version": "1.0.0",
  "description": "Daily scheduled Playwright smoke tests — CI/CD demo",
  "scripts": {
    "test": "npx playwright test",
    "report": "npx playwright show-report report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}